[
    { "method": "POST", "url": "/api/v4/agents/buildmapping", "description": "Build mapping from the agent. Will return 400/409 if there are errors in the build file." },
    { "method": "POST", "url": "/api/v3/agents/buildmapping", "description": "Build mapping from the agent." },
    { "method": "POST", "url": "/api/v1/agents/blobs/:buildSessionId/:blobId", "description": "Saves a blob file by BSID. Used to store data for line information by the .NET agent or 'sl-mapping' file by the JS agent." },
    { "method": "GET", "url": "/api/v1/agents/blobs/:buildSessionId/:blobId", "description": "Gets a blob file by BSID. Blob can be a binary file with line information (.NET agent) or 'sl-mapping' file (JS Agent)." },
    { "method": "GET", "url": "/api/v1/agents/blobs/:buildSessionId", "description": "" },
    { "method": "POST", "url": "/api/v2/agents/events", "description": "Used to post a list of test events from the agent (Test Start/End etc..)" },
    { "method": "POST", "url": "/api/v3/agents/agent-events", "description": "Used to post a list agent events from the agent (used by the Cockpit)" },
    { "method": "POST", "url": "/api/v3/agents/buildend", "description": "Used to report build end and its result (success/failure)" },
    { "method": "GET", "url": "/api/v2/config", "description": "Gets remote config" },
    { "method": "POST", "url": "/api/v2/logsubmission", "description": "Used to post logs from the agent" },
    { "method": "POST", "url": "/api/v2/agents/buildsession", "description": "Used to create a BSID" },
    { "method": "GET", "url": "/api/v2/agents/buildsession/:buildSessionId", "description": "Used to get details by BSID" },
    { "method": "POST", "url": "/api/v5/agents/footprints", "description": "Used to submit footprints" },
    { "method": "POST", "url": "/api/v1/agents/productiondata/bsid/:buildSessionId", "description": "Used to submit production data (Java Agent Only)" },
    { "method": "GET", "url": "/api/v3/testExecution", "description": "Used to query execution status (open/pending delete/delete)" },
    { "method": "GET", "url": "/api/v1/agents/:agentType/recommended", "description": "Returns a recommended agent (unsecured, used by the Browser Agent)" },
    { "method": "GET", "url": "/api/v2/agents/:agentType/recommended", "description": "Returns a recommended agent version" },
    { "method": "GET", "url": "/api/v1/lab-ids/:labId/build-sessions/active", "description": "Returns a list of BSID assigned for a lab (BSID Simplification)" },
    { "method": "GET", "url": "/api/v2/test-exclusions/:buildSessionId/:testStage", "description": "Returns a list of tests to be excluded by the agent (for TIA)" },
    { "method": "GET", "url": "/api/v4/testExecution/:labId", "description": "Used to query execution status (open/pending delete/delete). Used by the browser agent." },
    { "method": "POST", "url": "/api/v6/agents/:executionBsId/footprints/:testStage/:buildSessionId", "description": "API for sending footprints in new format" }
]
