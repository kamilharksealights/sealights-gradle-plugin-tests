#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var body_parser_1 = __importDefault(require("body-parser"));
var express_1 = __importDefault(require("express"));
var router_1 = require("./router");
var path_1 = require("path");
var app = express_1.default();
var port = parseInt(process.env.SERVER_PORT || '9900');
var client = path_1.resolve(__dirname, '../web');
/**
 * Configure the server
 */
function configureServer() {
    app.use(body_parser_1.default.json({ limit: '150mb' }));
    app.use(body_parser_1.default.raw({ limit: '150mb' }));
    app.use(router_1.MockRouter.router);
    app.set('view engine', 'ejs');
    app.set('views', client);
    app.use(express_1.default.static(client));
}
/**
 * Start the server
 */
function startServer() {
    app.listen(port, function (error) {
        if (error) {
            console.log(error);
        }
        else {
            console.log(new Date() + " SeaLights new mock server is listening at port " + port);
        }
    });
}
configureServer();
router_1.MockRouter.configure();
startServer();
